#include<stdio.h>
int main()
{
	int n;
	scanf_s("%d", &n);

	if (n % 2 == 0) {
		for (int i = 1; i <= n; i++) {
			printf("%d ", 2 * i);
		}
	}
	else if (n % 2 != 0) {
		for (int i = 1; i <= n; i++) {
			printf("%d ", 2 * i - 1);
		}
	}

	return 0;
}
